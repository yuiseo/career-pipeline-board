# DECISIONS — 설계 결정 기록

## 1. 요구사항 가정

요구사항이 모호한 부분에 대해 세운 가정입니다. 사용자 확인 전인 가정·질문은 `(기본값 처리 가능)` 또는 `(방향 확인 필요)`로 표시하고, 확정되면 표시를 지운다.

- **불합격은 전용 컬럼으로 둔다.** 불합격은 서류검토·면접·처우협의 중 어느 단계에서든 이동될 수 있는 상태이므로, 카드의 상태 플래그가 아니라 독립된 컬럼이 필요하다고 판단함.
  - 컬럼 순서: 서류검토 → 면접 → 처우협의 → 최종합격, 그리고 별도의 불합격 컬럼
- **카드 표시 항목은 가이드가 명시한 이름·직무·지원일·현재 단계로 한정한다.** 검색은 이름, 필터는 직무를 대상으로 한다.
- **상단 바 좌우 여백은 32px로 둔다.** DESIGN.md에 값이 없어 보드 영역 좌우 여백(32px)과 왼쪽 끝을 맞추는 기본값으로 처리했고, 브라우저로 화면을 확인해 확정함.
- **컬럼 사이 간격은 12px로 둔다.** DESIGN.md에 값이 없어 정함. 카드 간격(8px)보다 넓어 컬럼 경계가 구분되고, 5개 컬럼이 1,440px 화면에 거의 한눈에 들어온다.
- **컬럼은 화면 높이까지 채우고 카드 목록만 컬럼 안에서 세로 스크롤한다.** 컬럼 헤더는 고정. DESIGN.md의 "+ N명 더" 표시는 Should(대량 데이터) 대비라 Must 단계에서는 제외하고, 가상 스크롤을 붙일 때 이 스크롤 영역을 그대로 사용한다.
- **빈 컬럼 문구는 "이 단계의 지원자가 없습니다"로 한다.** DESIGN.md("이 단계엔 지원자가 없어요")와 FUNCTIONAL_SPEC FR-06이 달라, 다른 빈 상태 문구와 어미가 맞는 FR-06 문구를 채택함.
- **(기본값 처리 가능)** 컬럼 헤더 여백(`px-1.5 py-1`), 카운트 배지(흰 배경 + 테두리, `--muted` 컬럼 배경 위에서 묻히지 않게), 빈 컬럼 문구 여백(`py-6`)은 DESIGN.md에 값이 없어 구현 시 정함. 브라우저 확인 후 확정한다.
- **상세 보기 항목은 가이드에 명시가 없어 임의로 정한다.** 채용 담당자가 보통 확인하려는 정보로 이메일·연락처·경력 연차·학력을 예상함. 처음에는 상세 보기 구현 단계에서 추가하기로 했으나, 구현 계획에서 Mock API 단계에 목록·상세 타입을 함께 정의하기로 변경함.
- **(기본값 처리 가능)** 카드 패딩(`p-2.5`)·행 간격(`gap-1.5`)은 DESIGN.md §3-3에 수치가 없어 정함. 지원일은 `YYYY-MM-DD` 그대로 표시한다.
- **(기본값 처리 가능)** 토스트 스택은 우하단(`fixed right-4 bottom-4`)에 둔다. DESIGN.md §4-4에 위치가 없어, 상단바·상세 패널과 겹치지 않는 기본값으로 정함. 브라우저 확인 후 확정한다.

## 2. 주요 설계 결정

<!--
결정마다 아래 형식으로 작성
### 2-N. 결정 제목
- 결정:
- 대안:
- 트레이드오프 / 선택 이유:
-->

### 2-1. 단계 이동 방식: 액션 버튼

- **결정:** 카드의 단계 이동은 명시적 액션 버튼(메뉴) 방식으로 구현한다.
- **대안:** 드래그앤드롭
- **트레이드오프 / 선택 이유:**
  - 목표는 우선 Must 요건에 맞춰 구현하는 것이다.
  - 드래그앤드롭이 사용자에게 편의성을 주지만, 제한 시간 안에 과제를 구현해야 한다. 드래그앤드롭을 택하면 키보드 대체 경로를 따로 만들어야 하고, 가상 스크롤과 함께 쓸 때 드래그 중인 카드가 언마운트되는 문제도 처리해야 한다.
  - 드래그앤드롭은 추후 발전 가능한 요건으로 남겨둔다.

### 2-2. mock API 규약: 목록 조회와 상세 조회 분리

- **결정:**
  - `GET /api/candidates`는 카드에 필요한 기본 정보(`id`, `stage`, `name`, `position`, `appliedAt`)만 반환한다.
  - `GET /api/candidates/:id`는 상세 보기용으로 따로 둔다.
  - `PATCH /api/candidates/:id/stage`로 단계를 변경한다.
  - 리소스 이름은 설계 초안의 `applicants`에서 `candidates`로 바꿨다(구현 계획 단계). `/api` 접두사로 앱 경로와 구분하고 MSW가 가로챌 범위를 명확히 한다.
  - 모든 요청에 200~800ms 무작위 지연과 15% 실패(500)를 적용한다.
  - 유효하지 않은 요청은 4xx로 구분한다. 존재하지 않는 지원자 ID는 `404`, 허용되지 않은 `stage` 값 등 잘못된 요청 본문은 `400`이며, 응답 형식은 `{ message }`로 같다. 데이터는 localStorage에 버전 키로 저장하고, 기본 시드는 1,000건이다.
  - 시연용 제어(URL 쿼리로 실패 강제·실패 없음·빈 데이터·초기화)를 미리 만들어 둔다.
  - 실제 백엔드가 없으므로 MSW와 시연 제어는 개발·배포 환경 모두에서 켠다. 배포 링크에서도 평가자가 롤백·빈 상태를 바로 재현할 수 있다.
  - `mockFail`은 요청 시점의 URL로 판단한다. `mockSeed=0`(빈 데이터)과 `mockReset=1`(시드 복구)은 저장 데이터를 바꾸고, 적용 후 URL에서 제거한다.
- **대안:** 목록 응답에 전체 필드를 담고, 상세 보기는 목록 캐시에서 꺼내 쓴다 (AI 제안).
- **트레이드오프 / 선택 이유:**
  - 추후 상세에서 볼 필드가 추가되면, 목록에 전체 필드를 두는 방식은 한 번에 더 많은 데이터를 불러와야 하는 문제가 생긴다.
  - 단건 조회를 따로 두면 실패 범위가 격리된다. 상세 조회가 실패해도 보드 목록에는 영향이 없다.
  - TanStack Query를 사용하므로 상세 데이터도 쿼리 캐시에 저장되어, 한 번 연 상세를 다시 열 때 재요청 비용이 줄어든다.
  - 기본 시드를 1,000건으로 두어 Must 기준(200건 이상)과 Should 기준(1,000건)을 같은 데이터로 검증한다.

### 2-3. 낙관적 업데이트 롤백 · 경쟁 상태: 카드별 요청 직렬화 + UI 상태와 확인된 서버 상태 분리

- **결정:** 카드별로 요청을 하나씩만 보내고(직렬화), 화면에 보이는 단계와 서버가 확인한 단계를 분리해 관리한다. 이 로직은 직접 구현한다.
- **핵심 시나리오:**
  1. 단일 요청 실패: 서류 → 면접, API 실패 → 서류로 rollback
  2. 연속 이동 성공: 서류 → 면접 → 처우, req1 성공 → req2 성공 → 최종 처우
  3. 연속 이동 중 첫 요청 실패: 서류 → 면접 → 처우, req1 실패 → 서류로 rollback, req2는 전송하지 않음
- **판정 기준:** 추가 시나리오도 아래 세 기준을 만족하는지로 판별한다.
  1. UI는 항상 가장 최근 사용자의 의도를 보여준다.
  2. 서버 요청은 지원자별로 동시에 하나만 실행한다.
  3. 요청이 실패하면 해당 지원자는 마지막으로 확인된(confirmed) 상태로 복구하고, pending intent를 버린다.
- **추가 시나리오:**
  4. 서류 → 면접 → 처우, req1 성공 → req2 실패 → 면접으로 rollback (서류가 아님)
  5. 면접 요청 진행 중 처우 → 최종합격 연속 선택 → pendingTarget이 최종합격으로 덮어써지고 처우는 전송하지 않음
  6. 면접 요청 진행 중 다시 서류 선택 → req1 성공 후 서류로 전송
  7. 요청 진행 중 목록 재조회로 이전 서버 데이터 도착 → 화면은 pending/inflight 목표를 우선 표시하므로 UI가 튀지 않음
  8. 이동 성공 후 상세 캐시의 stage → TanStack Query로 상세 캐시 값을 변경
- **상태 모델:** 지원자별로 진행 중인 요청 목표와 대기 중인 목표만 저장한다.
  ```ts
  type CandidateStageState = {
    inFlightTarget?: Stage  // 현재 서버에 요청 중인 단계
    pendingTarget?: Stage   // 요청 처리 중 추가로 선택된 가장 최근 단계
  }
  ```
  - 처음에는 `optimisticStage`(화면 표시 단계), `inFlight: boolean`을 함께 저장하는 모델을 검토했다. 하지만 `optimisticStage`는 다른 필드와 값이 어긋나 저장 값의 정합성을 유지하기 어렵다. 그래서 계산 값으로 파생하고, `inFlight`는 `inFlightTarget: Stage`로 바꿨다.
- **화면 표시 단계 계산 우선순위:** `pendingTarget` → `inFlightTarget` → 서버에서 마지막으로 확인된 단계
- **세부 규칙:**
  - 진행 중인 요청이 없고 현재 표시 단계와 같은 단계를 선택하면 요청을 만들지 않는다.
  - 진행 중이던 요청이 성공했을 때 `pendingTarget`이 확인된 단계와 같으면 전송하지 않고 상태를 비운다.
- **대안:**
  - 요청 직전 캐시 스냅샷으로 롤백 (TanStack Query 문서 예제 방식): 연속 이동 시 오래된 스냅샷으로 되돌아가 서버 상태와 어긋남
  - 서버 버전 검사(`version` 필드, 409): mock 서버 로직과 충돌 처리 흐름이 늘어남
  - TanStack Query `scope` 옵션으로 직렬화: 핵심 로직을 라이브러리에 맡기게 되고, 대기 중인 요청의 낙관적 반영 시점을 따로 검증해야 함
- **트레이드오프 / 선택 이유:** 롤백 기준을 요청 직전 스냅샷이 아니라 확인된 서버 상태로 두면 위 3가지 시나리오를 일관되게 처리할 수 있다. UI 상태와 확인된 서버 상태를 분리한 모델이 가장 깔끔하다고 판단함.

### 2-4. 로딩 · 에러 · 빈 상태와 재시도 정책

- **결정:**
  - 조회(query)는 실패 시 1회 자동 재시도한다.
  - 단계 이동(mutation)은 자동 재시도하지 않는다. 실패하면 사용자에게 명시적으로 알리고 다시 시도하도록 안내한다.
  - 빈 상태는 3단계로 구분한다.
    1. 전체 0건: 등록된 지원자가 없음을 표시
    2. 검색·필터 결과 0건: 조건에 맞는 지원자가 없음을 표시하고 필터 초기화를 유도
    3. 특정 컬럼 0건: 이 단계의 지원자 없음을 표시
- **대안:**
  - 조회도 재시도 없음: 15% 확률 그대로 에러 화면이 자주 노출됨
  - TanStack Query 기본값(3회): 에러 상태가 거의 드러나지 않고 로딩이 길어짐
- **트레이드오프 / 선택 이유:**
  - 조회는 1회 재시도로 일시적 실패를 흡수하되, 연속 실패(약 2%)는 에러 상태로 드러난다.
  - 이동 실패는 사용자의 의도가 반영되지 않은 것이므로, 자동 재시도로 숨기기보다 명시적으로 알리고 다시 시도하게 하는 것이 알맞다고 판단함. 자동 재시도는 2-3의 지원자별 직렬화 흐름과도 섞인다.

### 2-5. 검색 · 필터

- **결정:**
  - 목록 캐시를 클라이언트에서 필터링한 뒤 컬럼별로 묶는다.
  - **이름 검색은 입력하는 대로 바로 결과가 바뀌지 않는다.** 검색 버튼을 누르거나 Enter를 입력했을 때 검색어가 적용되도록 유도한다.
  - 이름은 앞뒤 공백을 제거한 부분 일치로 찾는다.
  - **직무 필터는 여러 개를 선택할 수 있고, 체크하는 즉시 적용한다.** 아무것도 선택하지 않으면 전체 직무로 본다. 이름과 직무 조건은 AND로, 선택한 직무끼리는 OR로 조합한다.
  - **URL 동기화는 처음부터 구현한다.** 적용된 검색어와 선택한 직무를 URL 쿼리(`?q=홍&position=frontend&position=backend`)에 담고 `history.replaceState`로 갱신한다. 새로고침하거나 링크를 공유해도 같은 조건이 복원된다.
  - 컬럼 헤더에 필터링된 인원수를 표시한다.
- **대안 (AI 제안):**
  - 입력 즉시 반영 + `useDeferredValue`(디바운스 없음)
  - 직무 단일 선택
  - URL 동기화를 시간이 남으면 하는 항목으로 두기
- **트레이드오프 / 선택 이유:**
  - 검색어는 사용자가 검색 버튼이나 Enter로 적용하는 시점에만 결과가 바뀐다. 따라서 입력 중 렌더링 부담을 줄이려는 디바운스나 `useDeferredValue`가 필요 없다.
  - 필터 상태 공유는 URL로 해결한다(SPA 채택 판단의 전제). 페이지가 하나라 라우터 없이 `replaceState`로 충분하고, 조건을 바꿀 때마다 방문 기록이 쌓이지 않는다.

### 2-6. 상세 보기: 슬라이드 아웃 패널

- **결정:** 카드를 클릭하면 화면 옆에서 슬라이드 아웃 패널로 지원자 상세를 보여준다.
- **대안:** 모달
- **트레이드오프 / 선택 이유:** 패널이 열린 상태에서도 보드의 맥락이 가려지지 않는다.

### 2-7. 커밋 · 테스트 진행 방식

- **결정:**
  - 구현 계획의 단계는 작업 순서이고, 커밋은 단계 안에서 기능별로 나눈다.
  - 테스트는 Must 기능을 모두 만든 뒤 `test(...)` 커밋으로 몰아서 작성한다. 어떤 테스트가 있어야 하는지는 체크리스트로 남기고, 부족한 통합 시나리오를 마지막에 보강한다.
  - 디자인은 Claude 시안을 먼저 만든 뒤 기본 UI를 구현한다.
  - 폴더는 기능 단위로 나눈다(`types/`, `mocks/`, `api/`, `features/{board, filters, candidate-detail, stage-move}/`). 상세 구조는 [docs/PLAN.md](./docs/PLAN.md)에 둔다.
  - 단계 이동은 `stage-move`(서버 응답 후 반영)와 `optimistic-update`(즉시 반영 + rollback) 두 커밋으로 나눠 발전 과정을 드러낸다. Must 단계에서는 요청 중인 카드의 이동 메뉴를 비활성화해 지원자별 동시 요청 1개를 보장하고, Should의 경쟁 상태 처리에서 `pendingTarget`을 추가하며 다시 활성화한다.
- **대안 (AI 제안):** 기능 커밋마다 테스트를 함께 작성(TDD)
- **트레이드오프:** 롤백 로직 등의 테스트 검증이 구현보다 늦어진다. 대신 구현 커밋과 테스트 커밋이 분리되어 히스토리에서 각각 드러난다.

## 3. AI 제안 채택 / 기각

| AI 제안 | 판단 | 이유 |
|---|---|---|
| mock API를 MSW로 구현 | 채택 | 평가 요소가 SSR이 아닌 클라이언트 상호작용·비동기 상태관리이므로 SPA + 브라우저 mock으로 충분 |
| 단계 이동을 액션 버튼 방식으로 구현 | 채택 | Must 요건 우선. 드래그앤드롭은 제한 시간을 고려해 추후 발전 요건으로 남김 |
| 롤백 모델에서 `optimisticStage`를 파생값으로, `inFlight`를 `inFlightTarget`으로 변경 | 채택 | 저장 값끼리 어긋날 여지를 없앰 (2-3) |
| 조회 1회 재시도, 이동은 재시도 없음 | 채택 | 이동 실패는 명시적 알림 + 다시 시도가 알맞음 (2-4) |
| 검색어 입력 즉시 반영 + `useDeferredValue` | 기각 | 검색 버튼 또는 Enter로 적용하도록 유도 (2-5) |
| 직무 필터 단일 선택 | 기각 | 여러 직무 선택 가능 (2-5) |
| URL 동기화를 여유 시간 항목으로 두기 | 기각 | 처음부터 구현 (2-5) |
| DESIGN.md 시안의 정방향 이동 제약(지나간 단계 비활성) | 기각 | 확정된 결정(모든 단계로 이동 가능, 2-3 시나리오 6·Undo)과 충돌. 기존 결정 유지 |
| DESIGN.md 시안의 Must 실패 토스트만 구현 | 기각 | 성공·실패 사용자 피드백 모두 Must(구현 계획 4단계) 유지 |
| 기능 커밋마다 테스트 함께 작성(TDD) | 기각 | Must 기능 완료 후 test 커밋으로 몰아서 작성, 체크리스트 + 통합 시나리오 보강 (2-7) |
| 목록 응답에 전체 필드를 담고 상세는 목록 캐시 사용 | 기각 | 필드 추가 시 목록 데이터가 커지고 실패 범위가 격리되지 않음. 목록·상세 조회 분리 (2-2) |

## 4. 못 끝낸 기능

| 기능 | 남긴 상태 | 이유 |
|---|---|---|
