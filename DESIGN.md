# DESIGN — 디자인 시스템 · 화면 명세

이 문서는 [TEST_GUIDE.md](./TEST_GUIDE.md)의 Must/Should 요구사항과 [DECISIONS.md](./DECISIONS.md)의 설계 결정을, 실제 화면으로 구현하기 전에 시각적으로 먼저 확정해 둔 것이다. Claude Design 캔버스로 시안을 그려 검토한 뒤 정리했다.

> 구현은 **Must 우선**으로 진행한다. 이 문서에는 Should(도전 요건)에 해당하는 화면·컴포넌트도 함께 정리해 두었으며, 각 항목에 `Must`/`Should` 태그를 붙여 구분한다.

## 0. 참고한 자료

- [TEST_GUIDE.md](./TEST_GUIDE.md) — Must/Should 요구사항 원문
- [DECISIONS.md](./DECISIONS.md) — 2-1(액션 버튼) ~ 2-6(상세 슬라이드 아웃) 설계 결정
- `package.json` — 이미 설치된 Tailwind CSS v4, shadcn/ui(radix-nova), TanStack Query, `@fontsource-variable/geist`
- `src/index.css` — 기존 shadcn/ui 뉴트럴 테마 토큰 (오클치 그레이스케일)
- `src/components/ui/button.tsx` — 버튼 variant·size 규칙 (높이 32px 기준 등)

## 1. 디자인 토큰

### 1-1. 기존 토큰 (변경 없음 — `src/index.css` 그대로 사용)

| 토큰                                 | 값                                      | 용도                       |
| ------------------------------------ | --------------------------------------- | -------------------------- |
| `--background` / `--foreground`      | `oklch(1 0 0)` / `oklch(0.145 0 0)`     | 페이지 배경·기본 텍스트    |
| `--card`                             | `oklch(1 0 0)`                          | 카드·패널 배경             |
| `--muted`                            | `oklch(0.97 0 0)`                       | 컬럼 배경, 보조 영역       |
| `--muted-foreground`                 | `oklch(0.556 0 0)`                      | 보조 텍스트(직무, 날짜 등) |
| `--border`                           | `oklch(0.922 0 0)`                      | 테두리, 구분선             |
| `--primary` / `--primary-foreground` | `oklch(0.205 0 0)` / `oklch(0.985 0 0)` | 기본 버튼                  |
| `--destructive`                      | `oklch(0.577 0.245 27.325)`             | 불합격·에러·삭제류 액션    |
| `--radius`                           | `0.625rem`(10px)                        | 카드·버튼 기준 라운드      |

### 1-2. 신규 토큰 — 단계 배지 색상 (구현 시 `index.css`에 추가 필요)

컬럼·배지를 단계별로 구분하기 위해 뉴트럴 팔레트에 낮은 채도(chroma ≈ 0.02~0.04)의 배지 색을 추가한다. 카드 테두리에는 적용하지 않는다 (좌측 컬러 보더 같은 장식은 쓰지 않음).

| 단계     | 배경(bg)               | 텍스트(text)           | 점(dot)                |
| -------- | ---------------------- | ---------------------- | ---------------------- |
| 서류검토 | `oklch(0.94 0.02 250)` | `oklch(0.42 0.09 253)` | `oklch(0.55 0.13 253)` |
| 면접     | `oklch(0.94 0.04 80)`  | `oklch(0.45 0.11 68)`  | `oklch(0.62 0.15 70)`  |
| 처우협의 | `oklch(0.94 0.02 300)` | `oklch(0.44 0.09 296)` | `oklch(0.55 0.13 298)` |
| 최종합격 | `oklch(0.94 0.04 150)` | `oklch(0.42 0.1 150)`  | `oklch(0.55 0.14 150)` |
| 불합격   | `oklch(0.94 0.02 25)`  | `oklch(0.45 0.15 27)`  | `--destructive` 재사용 |

## 2. 타이포그래피

- 폰트: **Geist Variable** (`@fontsource-variable/geist`, 이미 설치됨). _시안(캔버스)에서는 외부 폰트 로드 제약으로 system-ui 계열로 대체했으나, 실제 구현에서는 프로젝트에 이미 설치된 Geist를 그대로 쓴다._
- 크기: 페이지 제목 18px/600 · 상세 패널 이름 17px/700 · 카드 이름 13.5px/600 · 컬럼 제목·본문 13px/600 · 보조 텍스트 12px · 배지·날짜 10.5~11px.

## 3. 레이아웃

### 3-1. 전체 구조

```
┌─ Topbar (72px, border-bottom) ──────────────────────────┐
│ 제목 · 부제              검색 [필드+버튼]  직무 필터 [드롭다운] │
├───────────────────────────────────────────────────────┤
│ Board (flex, overflow-x: auto, padding 22px 32px)        │
│  [컬럼] [컬럼] [컬럼] [컬럼] [컬럼]                        │
└───────────────────────────────────────────────────────┘
```

### 3-2. 컬럼 — `Must`

- 너비 268px 고정, 배경 `--muted`, `border-radius` 12px(시안 값 — **구현 시 프로젝트 `--radius`(10px)로 통일 권장**), 내부 패딩 6px.
- 헤더: 단계 점(8px) + 이름(13px/600) + 우측 카운트 배지(전체 건수, 필터링된 건수 아님 — 실제로는 필터링 후 건수를 표시).
- 본문: 카드 목록(세로 스택, gap 8px). 화면에 다 안 들어오는 나머지는 컬럼 하단에 `+ N명 더` 형태로 표시해 스크롤 가능함을 암시한다.

### 3-3. 카드 — `Must`

앤아토미: 상단 행(이름 13.5px/600 + 직무 12px, 우측에 단계 변경 트리거) / 하단 행(지원일 11px + 단계 배지).

카드 상태:

| 상태                           | 시각 표현                                                                   | 관련 결정                      |
| ------------------------------ | --------------------------------------------------------------------------- | ------------------------------ |
| 기본                           | 흰 배경, 얇은 테두리, `box-shadow` 매우 약하게                              | —                              |
| 단계 변경 메뉴 열림            | 카드 아래에 드롭다운 메뉴 표시                                              | 2-1                            |
| 요청 진행 중(`inFlightTarget`) | 메뉴 트리거 자리에 스피너 + "이동 중" 배지, 대상 단계 색으로 배지 미리 표시 | 2-3 `Must`/`Should`(경쟁 상태) |
| 키보드 포커스                  | `outline: 2px solid var(--ring); outline-offset: 2px`                       | `Should`(접근성)               |
| 상세 열림(선택됨)              | `--ring` 색 3px 링 강조                                                     | 2-6                            |

## 4. 컴포넌트별 명세

### 4-1. 검색 · 필터 — `Must` (2-5)

- **이름 검색**: 텍스트 필드 + 별도 검색 아이콘 버튼(36×36). **입력 즉시 반영하지 않는다** — 버튼 클릭 또는 Enter로만 검색어가 적용된다. 디바운스/`useDeferredValue`는 쓰지 않는다.
- **직무 필터**: "직무 필터" 버튼(+선택 개수 배지) → 클릭 시 체크박스 다중 선택 드롭다운. **체크하는 즉시 적용**되고, 하단에 "선택 해제" 링크를 둔다. 아무것도 선택하지 않으면 전체 직무로 간주한다.
- 적용된 검색어·선택 직무는 URL 쿼리(`?q=...&position=...`)에 동기화한다(화면 요소는 아니므로 시안에는 별도 표기 없음).

### 4-2. 단계 변경 메뉴 — `Must` (2-1, 이 프로젝트의 핵심 제약)

카드의 단계 변경 트리거를 클릭하면 아래 구조의 드롭다운이 열린다.

1. 안내 문구: "지나간 단계로는 되돌릴 수 없어요" (자물쇠 아이콘)
2. 구분선
3. **지나간 단계**: 비활성(회색, `opacity: 0.55`) + 체크 아이콘 + "완료" 태그. **클릭 불가.**
4. **현재 단계**: `--muted` 배경 강조 + "현재" 태그. 클릭 불가(같은 단계 재요청 방지, 2-3 세부 규칙과 일치).
5. **다음 단계들**: 활성 상태로 순서대로 노출.
6. 구분선
7. **불합격 처리**: `--destructive` 색, 어느 단계에서든 활성.

이 메뉴는 지나간 단계 항목을 아예 숨기지 않고 **비활성 표시로 남겨** "왜 안 되는지"를 사용자가 바로 이해하게 한다. 정방향 제약은 메뉴 구성 자체로 강제된다.

### 4-3. 상세 슬라이드 아웃 패널 — `Must` (2-6)

- 우측에서 슬라이드 아웃, 너비 420px, 배경 스크림(`oklch(0 0 0 / 0.38)`)으로 보드 맥락은 흐리게 유지.
- 헤더: 아바타(이니셜) + 이름(17px/700) + 직무 + 단계 배지. **목록 쿼리 캐시에 이미 있는 필드**(이름·직무·단계·지원일)이므로 상세 조회 성공 여부와 무관하게 즉시 렌더된다 (2-2).
- 본문: 정보 목록(이메일 · 연락처 · 경력 연차 · 최종학력 · 지원일). 이메일·연락처·경력·학력은 `GET /api/applicants/:id` 응답으로만 채워진다.
  - **로딩 중**: 위 4개 필드만 스켈레톤으로 표시.
  - **에러**: 정보 목록 자리에 인라인 에러 박스("상세 정보를 불러오지 못했습니다" + 다시 시도) 노출. **헤더와 하단 단계 변경 버튼은 계속 동작** — 상세 조회 실패가 목록·단계 변경에 영향을 주지 않는다(2-2의 목록/상세 분리 이유가 그대로 드러나는 지점).
- 푸터: 단계 변경 버튼 + 안내 문구("현재 단계보다 앞선 단계로만 이동할 수 있어요").

### 4-4. 토스트

공통 컴포넌트 하나를 공유하고, 아이콘 색·문구·액션 버튼만 유형별로 다르다.

| 유형                  | 우선순위       | 아이콘 색     | 액션        |
| --------------------- | -------------- | ------------- | ----------- |
| 실패 · 롤백           | `Must`(3)      | 빨강 계열     | "다시 시도" |
| 성공 · 실행 취소 가능 | `Should`(Undo) | 초록 계열     | "실행 취소" |
| 실행 취소 완료        | `Should`(Undo) | 중립(흰색 톤) | 없음        |

지금은 실패·롤백 토스트만 구현한다. 성공/실행취소 완료는 Undo를 붙일 때 같은 컴포넌트에 variant만 추가하면 된다.

### 4-5. 로딩 · 에러 · 빈 상태 — `Must` (2-4)

- **로딩**: 컬럼 헤더·카드 자리를 shimmer 스켈레톤으로 대체. 조회는 실패 시 1회 자동 재시도하므로, 재시도 중에도 같은 로딩 화면을 유지한다.
- **에러(목록 조회)**: 1회 자동 재시도 후에도 실패했을 때만 노출. 중앙 정렬 아이콘 + 안내 문구 + "다시 시도" 버튼.
- **빈 상태 3단계**:
  1. **전체 0건** — 등록된 지원자가 아예 없음("등록된 지원자가 없습니다").
  2. **검색·필터 결과 0건** — 적용된 검색어/필터 조건 표시 + "검색·필터 초기화" 버튼.
  3. **컬럼 0건** — 다른 컬럼은 정상, 특정 단계만 카드 없음("이 단계엔 지원자가 없어요"). 컬럼 내부에만 표시하고 보드 전체를 비우지 않는다.

## 5. 접근성 — `Should`

- 드래그앤드롭이 아닌 **액션 버튼 방식**(2-1)을 택했으므로, 카드 탐색과 단계 변경 모두 키보드만으로 가능해야 한다: `Tab`으로 카드/버튼 이동, `Enter`/`Space`로 메뉴 열기·항목 선택.
- 포커스 표시는 기존 `--ring` 토큰을 재사용한 `outline`으로 통일한다 (별도 색상 추가 없음).

## 6. Must / Should 매핑

| 화면·컴포넌트                             | 요구사항           | 구분        |
| ----------------------------------------- | ------------------ | ----------- |
| 보드 · 컬럼 · 카드                        | 1                  | Must        |
| 단계 변경 메뉴(정방향 제약)               | 2                  | Must        |
| 요청 진행 중 카드 상태, 실패·롤백 토스트  | 3                  | Must        |
| 검색(버튼/Enter 적용) · 직무 다중 필터    | 4                  | Must        |
| 상세 슬라이드 아웃 패널                   | 5                  | Must        |
| 로딩 스켈레톤 · 에러 화면 · 빈 상태 3단계 | 6                  | Must        |
| 카드 "이동 중" 표시(직렬화 근거)          | 도전 1 (경쟁 상태) | Should      |
| 컬럼 "+N명 더" (대량 데이터 스크롤)       | 도전 2 (성능)      | Should 대비 |
| 성공/실행취소 완료 토스트                 | 도전 3 (Undo)      | Should      |
| 키보드 포커스 링                          | 도전 5 (접근성)    | Should      |

## 7. 구현 메모

- 색상·라운드 등은 `src/index.css`의 기존 shadcn 토큰을 **확장**하는 방식으로 추가한다(1-2의 단계 배지 색만 신규). 기존 토큰 값을 바꾸지 않는다.
- 버튼은 `src/components/ui/button.tsx`의 기존 variant(`default`/`outline`/`ghost`/`destructive`)·size(`default`=32px 등)를 그대로 사용하고, 새 variant를 추가하지 않는다.
- 컬럼 `border-radius`는 시안에서 12px을 썼지만, 구현 시에는 프로젝트 `--radius`(10px)로 통일한다 — 시안 제작 중 발견한 사소한 불일치이며 의도적 차이가 아니다.
